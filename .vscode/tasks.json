{
  "tasks": [
      {
          "type": "cppbuild",
          "label": "build debug",
          "command": "make",
          "args": [
              "Debug",
              "--directory=${workspaceFolder}"
          ],
          "options": {
              "cwd": "${fileDirname}"
          },
          "group": {
              "kind": "build",
              "isDefault": true
          }
      },

      {
        "type": "cppbuild",
        "label": "build",
        "command": "make",
        "args": [
            "--directory=${workspaceFolder}"
        ],
        "options": {
            "cwd": "${fileDirname}"
        },
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }
  ],
  "version": "2.0.0"
}